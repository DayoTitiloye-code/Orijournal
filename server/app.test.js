// const { it } = require("node:test");
const request = require("supertest");
// const { describe } = require("yargs");
const app = require("./app")

describe("Todos", () => {
    it("GET / --> array data",()=> {
    })

    // it("POST /community --> created community",()=> {
    // })

    // it("GET /community --> 404 if not found ", () =>{})

    // it("GET /community --> validate request body",()=> {
    // })
})
